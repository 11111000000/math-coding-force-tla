---
description: Regenerate refinement.md and traceability.json from a TLA+ model
---

Run `./bin/refine-from-model` on the given packet.

Then complete the generated refinement.md by filling in target-language mappings. After code is generated, update traceability.json with concrete targets.

Arguments: $ARGUMENTS