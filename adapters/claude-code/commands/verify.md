---
description: Run mechanical verification on a MathCoding packet
---

Run `./bin/verify` on the given packet and report the verdict.

If verdict is not VERIFIED, surface the counterexample or failure and explain what the model needs to change.

Arguments: $ARGUMENTS