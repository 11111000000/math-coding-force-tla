#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -ne 1 ]; then
  printf 'Usage: %s <artifacts/task-id>\n' "$0" >&2
  exit 1
fi

PACKET_DIR="$1"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/validate-packet.py" "$PACKET_DIR"